{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputMode": "new-only",
      "outputs": ["dist/**", "types/**"]
    },
    "test": {},
    "dev": {
      "dependsOn": ["^dev"]
    }
  }
}
