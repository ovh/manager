{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "types/**"],
      "outputLogs": "new-only"
    },
    "test": {},
    "dev": {
      "dependsOn": ["^dev"]
    },
    "start": {
      "persistent": true,
      "cache": false,
      "env": ["VITE_CONTAINER_APP", "CONTAINER"],
      "dependsOn": ["^dev"]
    }
  }
}
