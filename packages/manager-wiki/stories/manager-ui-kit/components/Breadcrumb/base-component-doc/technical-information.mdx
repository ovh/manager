import { Canvas, Meta } from '@storybook/blocks';
import * as SpecificationsBreadcrumb from '@ovhcloud/ods-react';
import { Banner } from '../../../base-documents/components/banner/Banner';
import { Canvas as OdsCanvas } from '../../../base-documents/components/canvas/Canvas';
import { Heading } from '../../../base-documents/components/heading/Heading';
import { TechnicalSpecification } from '../../../base-documents/components/technicalSpecification/TechnicalSpecification';
import * as BreadcrumbStories from './breadcrumb.stories';

<Meta of={ BreadcrumbStories } name="Technical information" />

<Banner of={ BreadcrumbStories } />

<Heading label="Overview" level={ 2 } />

<Canvas of={ BreadcrumbStories.Overview } sourceState="none" />

<Heading label="Anatomy" level={ 2 } />

![Component anatomy](./base-assets/components/breadcrumb/anatomy-tech.png "Component anatomy")

1. **Breadcrumb**
2. **Breadcrumb Item**
3. **Breadcrumb Link**

<TechnicalSpecification
  data={ SpecificationsBreadcrumb }
  extraInfo={{
    'Breadcrumb': {
      extendAttribute: {
        name: 'nav',
        url: 'https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/nav#attributes',
      },
    },
    'BreadcrumbItem': {
      extendAttribute: {
        name: 'li',
        url: 'https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/li#attributes',
      },
    },
    'BreadcrumbLink': {
      extendAttribute: {
        name: 'a',
        url: 'https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/a#attributes',
      },
    }
  }}
  of={ BreadcrumbStories } />

<Heading label="Examples" level={ 2 } />

<Heading label="Default" level={ 3 } />

<OdsCanvas of={ BreadcrumbStories.Default } sourceState="shown" />

<Heading label="Collapsed" level={ 3 } />

<OdsCanvas of={ BreadcrumbStories.Collapsed } sourceState="shown" />

You can customize the number of item displayed before and after the ellipsis.

<OdsCanvas of={ BreadcrumbStories.CustomAfterBeforeCollapse } sourceState="shown" />

You can also customize the threshold from when the breadcrumb will collapse.

<OdsCanvas of={ BreadcrumbStories.CustomCollapseThreshold } sourceState="shown" />

Or you can even deactivate the whole collapse logic.

<OdsCanvas of={ BreadcrumbStories.NoCollapse } sourceState="shown" />
