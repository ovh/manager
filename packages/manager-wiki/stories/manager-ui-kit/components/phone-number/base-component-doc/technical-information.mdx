import { Canvas, Meta } from '@storybook/blocks';
import * as SpecificationsPhoneNumber from '@ovhcloud/ods-react';
import { Banner } from '../../../base-documents/components/banner/Banner';
import { Canvas as OdsCanvas } from '../../../base-documents/components/canvas/Canvas';
import { Heading } from '../../../base-documents/components/heading/Heading';
import { StorybookLink } from '../../../base-documents/components/storybookLink/StorybookLink';
import { TechnicalSpecification } from '../../../base-documents/components/technicalSpecification/TechnicalSpecification';
import { HOME_TITLE, REACT_COMPONENTS_TITLE, STORY } from '../../../base-documents/constants/meta';
import * as PhoneNumberStories from './phone-number.stories';

<Meta of={ PhoneNumberStories } name="Technical information" />

<Banner of={ PhoneNumberStories } />

<Heading label="Overview" level={ 2 } />

<Canvas of={ PhoneNumberStories.Overview } sourceState="none" />

<Heading label="Anatomy" level={ 2 } />

![Component anatomy](/assets/base-assets/components/phone-number/anatomy-tech.png "Component anatomy")

1. **Phone Number**
2. **Phone Number Country List**
3. **Phone Number Control**

<TechnicalSpecification
  data={ SpecificationsPhoneNumber }
  extraInfo={{
    'PhoneNumber': {
      extendAttribute: {
        name: 'div',
        url: 'https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/div#attributes',
      },
    },
    'PhoneNumberControl': {
      extendAttribute: {
        name: 'input',
        url: 'https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes',
      },
    },
    'PhoneNumberCountryList': {
      extendAttribute: {
        name: 'div',
        url: 'https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/div#attributes',
      },
    },
  }}
  of={ PhoneNumberStories } />

<Heading label="Examples" level={ 2 } />

<Heading label="Default" level={ 3 } />

<OdsCanvas of={ PhoneNumberStories.Default } sourceState="shown" />

<Heading label="Disabled" level={ 3 } />

<OdsCanvas of={ PhoneNumberStories.Disabled } sourceState="shown" />

<Heading label="Readonly" level={ 3 } />

<OdsCanvas of={ PhoneNumberStories.Readonly } sourceState="shown" />

<Heading label="Clearable" level={ 3 } />

<OdsCanvas of={ PhoneNumberStories.Clearable } sourceState="shown" />

<Heading label="Loading" level={ 3 } />

<OdsCanvas of={ PhoneNumberStories.Loading } sourceState="shown" />

<Heading label="Locale" level={ 3 } />

The locale manages the translation of the country list.

For further explanation about locale strategy, see <StorybookLink kind={ REACT_COMPONENTS_TITLE.phoneNumber } story={ STORY.documentation }>the locale behavior documentation</StorybookLink>.

<OdsCanvas of={ PhoneNumberStories.Locale } sourceState="shown" />

<Heading label="Country list" level={ 3 } />

You can use update the list of country to select from.

By default, the list use the `all` preset that displays all supported iso codes. But you can pass a specific subset to limit the possible choices.

For further explanation about country iso code strategy, see <StorybookLink kind={ REACT_COMPONENTS_TITLE.phoneNumber } story={ STORY.documentation }>the iso code behavior documentation</StorybookLink>.

<OdsCanvas of={ PhoneNumberStories.CountryList } sourceState="shown" />

<Heading label="Form field" level={ 3 }>
  <StorybookLink title={ HOME_TITLE.handlingFormGuide }>(see handling form guide)</StorybookLink>
</Heading>

<OdsCanvas of={ PhoneNumberStories.InFormField } sourceState="shown" />
