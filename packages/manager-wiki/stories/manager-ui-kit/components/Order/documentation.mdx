import { Meta, Canvas, Source } from '@storybook/blocks';

<Meta title="Manager UI Kit/Components/Order/Documentation" />

# Order

The `Order` component is a helper to build your template easily.

## Guideline

As the component `orderSummary` already use `<h2>` please don't use this tag for a correct semantic HTML structure.

