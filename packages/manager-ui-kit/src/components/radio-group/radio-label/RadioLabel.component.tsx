import { RadioLabel as ODSRadioLabel } from '@ovhcloud/ods-react';

import { RadioLabelProps } from './RadioLabel.props';

export const RadioLabel = (props: RadioLabelProps) => <ODSRadioLabel {...props} />;
