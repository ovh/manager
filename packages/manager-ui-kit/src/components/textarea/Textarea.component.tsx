import { Textarea as OdsTextarea } from '@ovhcloud/ods-react';

import { TextareaProps } from '@/components/textarea/Textarea.props';

export const Textarea = (props: TextareaProps) => <OdsTextarea {...props} />;
