import { PropsWithChildren } from 'react';

import { TabProp as OdsTabProps } from '@ovhcloud/ods-react';

/**
 * External types (from contexts, utils, or shared ODS exports)
 */
export type TabProps = PropsWithChildren<OdsTabProps> & {};
