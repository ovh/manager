import { ComboboxContent as ODSComboboxContent } from '@ovhcloud/ods-react';

import { ComboboxContentProps } from './ComboboxContent.props';

export const ComboboxContent = (props: ComboboxContentProps) => <ODSComboboxContent {...props} />;
