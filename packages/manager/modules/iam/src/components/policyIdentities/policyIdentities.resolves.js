import {
  alertResolve,
  goBackResolve,
  goToResolve,
  onboardingGuidesResolve,
  policyParamResolve,
} from '@iam/resolves';

export default [
  alertResolve,
  goBackResolve,
  goToResolve,
  onboardingGuidesResolve,
  policyParamResolve,
];
