export const STATE_NAME = 'nasha.dashboard.partitions';
export const INSTANCE_STATE_NAME = `${STATE_NAME}.partition`;
export const MAX_PARTITIONS = 10;
export const TASK_TRACKER_STATE_NAME = `${STATE_NAME}.task-tracker`;

export default {
  INSTANCE_STATE_NAME,
  MAX_PARTITIONS,
  STATE_NAME,
  TASK_TRACKER_STATE_NAME,
};
