export const EVENT_NAMESPACE = 'ovhManagerSupport';

export const EVENTS = {
  startLoading: `${EVENT_NAMESPACE}.startLoading`,
  stopLoading: `${EVENT_NAMESPACE}.stopLoading`,
};

export const STATE_NAME = 'app.account.support';

export default {
  EVENT_NAMESPACE,
  EVENTS,
  STATE_NAME,
};
