import angular from 'angular';

import supportTickets from './support-tickets';

const moduleName = 'ovhManagerSupportComponents';

angular.module(moduleName, [supportTickets]);

export default moduleName;
