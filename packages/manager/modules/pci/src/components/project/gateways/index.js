import angular from 'angular';

import gatewayModelSelector from './gateways-model-selector';

const moduleName = 'ovhManagerPciComponentsGateways';

angular.module(moduleName, [gatewayModelSelector]);

export default moduleName;
