import controller from './api-url.controller';
import template from './api-url.html';

const component = {
  bindings: {
    trackClick: '<',
  },
  controller,
  template,
};

export default component;
