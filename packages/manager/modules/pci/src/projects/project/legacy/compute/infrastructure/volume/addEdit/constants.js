export const CLOUD_VOLUME_MAX_SIZE = 4 * 1000; // Should be 10 * 1024 (but API is wrong;
export const CLOUD_VOLUME_MIN_SIZE = 10; // 10 Gio
export const CLOUD_VOLUME_UNLIMITED_QUOTA = -1; // Should be 10 * 1024 (but API is wrong)

export default {
  CLOUD_VOLUME_MAX_SIZE,
  CLOUD_VOLUME_MIN_SIZE,
  CLOUD_VOLUME_UNLIMITED_QUOTA,
};
