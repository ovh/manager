import angular from 'angular';

import activateProjectBanner from './activate-project-banner';

const moduleName = 'pciProjectComponents';

angular.module(moduleName, [activateProjectBanner]);

export default moduleName;
