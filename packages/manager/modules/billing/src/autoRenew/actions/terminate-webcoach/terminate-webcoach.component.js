import controller from './terminate-webcoach.controller';
import template from './terminate-webcoach.html';

export default {
  bindings: {
    goBack: '<',
    terminateWebCoach: '<',
  },
  controller,
  template,
};
