export const AVAILABLE_FILTERS = {
  region: 'region',
  sub: 'sub',
};

// Character used to name the property saying how data are filtered for this constant
export const FILTER_CHARACTER = '_';

export default {
  AVAILABLE_FILTERS,
  FILTER_CHARACTER,
};
