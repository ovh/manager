import { PREFIX_TRACKING_NUTANIX_POPUP } from '../../constants';

export const TRACKING = {
  LISTING_ADD_NODE: `${PREFIX_TRACKING_NUTANIX_POPUP}::general-information::order-nodes`,
};

export default {
  TRACKING,
};
