import component, { name } from './live-tail.component';

const moduleName = 'ovhManagerCloudConnectLogsLiveTail';
angular.module(moduleName, []).component(name, component);

export default moduleName;
