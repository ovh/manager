export const ENTERPRISE_CLOUD_DATABASE_MIN_PASSWORD_STRENGTH = 4;

export const ENTERPRISE_CLOUD_DATABASE_PASSWORD_LENGTH = {
  MIN: 12,
  MAX: 32,
};

export const ENTERPRISE_CLOUD_DATABASE_PASSWORD_PATTERNS = {
  ALPHA_NUMERIC: '^[a-zA-Z0-9]+$',
  UPPER_CASE: '[A-Z]+',
  NUMBER: '((?=.*\\d.*\\d).{2,})',
};

export default {
  ENTERPRISE_CLOUD_DATABASE_MIN_PASSWORD_STRENGTH,
  ENTERPRISE_CLOUD_DATABASE_PASSWORD_LENGTH,
  ENTERPRISE_CLOUD_DATABASE_PASSWORD_PATTERNS,
};
