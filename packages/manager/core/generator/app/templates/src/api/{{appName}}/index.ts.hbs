import apiClient from '@ovh-ux/manager-core-api';

export const getVpsIds = async (): Promise<string[]> => {
  const response = await apiClient.v6.get('{{ apiPath }}');
  return response.data;
};

export default {
  getVpsIds,
};
