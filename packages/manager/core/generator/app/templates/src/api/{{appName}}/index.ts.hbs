import apiClient from '@ovh-ux/manager-core-api';

export const get{{pascalCase appName}}Ids = async (): Promise<string[]> => {
  const response = await apiClient.v6.get('{{ apiPath }}');
  return response.data;
};

export default {
  get{{pascalCase appName}}Ids,
};
