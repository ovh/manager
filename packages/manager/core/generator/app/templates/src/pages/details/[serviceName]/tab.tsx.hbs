import i18next from 'i18next';
import React from 'react';
import { useTranslation } from 'react-i18next';

export function breadcrumb() {
  return i18next.t('{{appName}}/details/tab:crumb');
}

const Header: React.FC<{children: React.ReactNode}> = ({children}) => (
  <h3>{children}</h3>
)

export default function {{pascalCase appName}}Tab() {
  const { t } = useTranslation('{{appName}}/details/tab');

  return (
    <>
      <Header>
        {t('title')}
      </Header>
      Rename me by:
      <ul>
        <li>changing my file name by the route path you want</li>
        <li>changing the translation path loaded at line 5</li>
        <li>changing the tab title in _layout.tsx file</li>
      </ul>
    </>
  );
}
