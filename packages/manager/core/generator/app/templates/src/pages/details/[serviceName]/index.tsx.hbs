import React from 'react';
import { useTranslation } from 'react-i18next';

const Header: React.FC<{children: React.ReactNode}> = ({children}) => (
  <h3>{children}</h3>
)

export default function {{pascalCase appName}}Dashboard() {
  const { t } = useTranslation('{{appName}}/details/dashboard');

  return (
    <div>
      <Header>
        {t('title')}
      </Header>
    </div>
  );
}
