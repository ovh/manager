import React, { Suspense } from 'react';

import { BaseLayout } from '@ovh-ux/manager-react-components';

export default function ListingPage() {
  return (
    <BaseLayout>
      <Suspense>LISTING PAGE</Suspense>
    </BaseLayout>
  );
}
