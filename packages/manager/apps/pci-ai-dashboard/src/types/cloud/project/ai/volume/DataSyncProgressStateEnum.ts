/** State of the progress sync */
export enum DataSyncProgressStateEnum {
  'DONE' = 'DONE',
  'ERROR' = 'ERROR',
  'FAILED' = 'FAILED',
  'INTERRUPTED' = 'INTERRUPTED',
  'QUEUED' = 'QUEUED',
  'RUNNING' = 'RUNNING',
}
