import angular from 'angular';

import routing from './portabilities-cancel.routing';

const moduleName =
  'ovhManagerTelecomTelephonyBillingAccountAdministrationPortabilitiesCancel';

angular.module(moduleName, []).config(routing);

export default moduleName;
