export default class TelecomTelephonyAliasConfigurationCallsFilteringDeleteCtrl {
  /* @ngInject */
  constructor($uibModalInstance, itemCount) {
    this.$uibModalInstance = $uibModalInstance;
    this.itemCount = itemCount;
  }
}
