import angular from 'angular';

import routing from './portabilities-attach.routing';

const moduleName =
  'ovhManagerTelecomTelephonyBillingAccountAdministrationPortabilitiesAttach';

angular.module(moduleName, []).config(routing);

export default moduleName;
