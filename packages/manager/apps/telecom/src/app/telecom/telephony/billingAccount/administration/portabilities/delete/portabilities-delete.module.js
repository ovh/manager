import angular from 'angular';

import routing from './portabilities-delete.routing';

const moduleName =
  'ovhManagerTelecomTelephonyBillingAccountAdministrationPortabilitiesDelete';

angular.module(moduleName, []).config(routing);

export default moduleName;
