import angular from 'angular';

import routing from './portabilities-relaunch.routing';

const moduleName =
  'ovhManagerTelecomTelephonyBillingAccountAdministrationPortabilitiesRelaunch';

angular.module(moduleName, []).config(routing);

export default moduleName;
