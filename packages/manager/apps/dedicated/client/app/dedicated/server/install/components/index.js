import angular from 'angular';

import optionsComponent from './options';

const moduleName = 'ovhManagerDedicatedServerInstallComponents';

angular.module(moduleName, [optionsComponent]);

export default moduleName;
