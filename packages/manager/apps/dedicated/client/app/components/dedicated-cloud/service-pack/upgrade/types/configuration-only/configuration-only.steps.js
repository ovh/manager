import configureAccess from '../../steps/configure-access';
import configureUsers from '../../steps/configure-users';
import validation from '../../steps/validation';

export default [configureAccess, configureUsers, validation];
