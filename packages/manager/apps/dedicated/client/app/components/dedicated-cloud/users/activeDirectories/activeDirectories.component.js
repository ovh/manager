import controller from './activeDirectories.controller';
import template from './activeDirectories.html';

export default {
  bindings: {
    dedicatedCloud: '<',
    productId: '<',
  },
  controller,
  template,
};
