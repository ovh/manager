export const TICKET_STATE = ['open', 'closed', 'archived', 'unknown'];

export const TICKET_LAST_MESSAGE_FROM = ['customer', 'support'];

export default {
  TICKET_STATE,
  TICKET_LAST_MESSAGE_FROM,
};
