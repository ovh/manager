import { ORDER_WEBHOSTING_TRACKING } from './domain-webhosting-order.constants';

export default class WebDomainWebhostingOrderController {
  /* @ngInject */
  constructor() {
    this.ORDER_WEBHOSTING_TRACKING = ORDER_WEBHOSTING_TRACKING;
  }
}
