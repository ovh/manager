export const PURGE_TYPE_ENUM = {
  ALL: 'all', // All is not part of purge enum options, here for consistency
  EXTENSION: 'extension',
  FOLDER: 'folder',
  REGEX: 'regex',
  URI: 'uri',
};

export default {
  PURGE_TYPE_ENUM,
};
