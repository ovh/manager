import { describe, it } from 'vitest';

/**
 * Sample unit test to make "test" command pass.
 * Further unit tests will be added in a separate PR.
 */
describe('manager-pci-common', () => {
  it('run unit tests', async () => {
    expect(true).toBe(true);
  });
});
