import { SecretConfig } from '@secret-manager/types/secret.type';

export const mockSecretConfigOkms: SecretConfig = {
  casRequired: true,
  deactivateVersionAfter: '11d11h11m11s',
  maxVersions: 5,
};
