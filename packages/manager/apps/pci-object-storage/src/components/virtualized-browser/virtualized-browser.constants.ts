/**
 * Constants for VirtualizedBrowser component
 */
export const VIRTUALIZED_BROWSER = {
  /** Default height of each row in pixels */
  DEFAULT_ROW_HEIGHT: 44,
  /** Default number of items to render outside the visible area */
  DEFAULT_OVERSCAN: 20,
  /** Default number of items from the end to trigger preload */
  DEFAULT_PRELOAD_THRESHOLD: 10,
  /** Default maximum height of the browser */
  DEFAULT_MAX_HEIGHT: '50vh',
} as const;
