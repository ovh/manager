import common from '../../public/translations/identity-access-management/Messages_fr_FR.json';
import error from '../../public/translations/error/Messages_fr_FR.json';
import tagManager from '../../public/translations/tag-manager/Messages_fr_FR.json';

export const translations = {
  common,
  'tag-manager': tagManager,
  error,
};

export const labels = {
  common,
  error,
  tagManager,
};
