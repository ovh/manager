/**
 * Public contact page, reachable without an account: the customer is still in
 * the sign-up flow when the SIRET lookup fails to return their organisation.
 * The company step is FR + DROM only, hence the FR page.
 */
export const SUPPORT_CONTACT_URL = 'https://www.ovhcloud.com/fr/contact/';
