export const urls = {
  root: '/',
  overview: '/overview',
  dashboards: '/dashboards',
  logs: '/logs',
  metrics: '/metrics',
  settings: '/settings',
};

export const OBS_URL_PARAMS = {
  productId: ':productId',
};

export const OBS_ROUTES_URLS = urls;
