import {
  StoryBookBanner,
  StorybookHeading,
  StorybookBestPractices,
  StorybookIdentityCard,
  StorybookTable,
} from '../../../.storybook/docs/components/';
import { Meta, Canvas, Source } from '@storybook/blocks';
import meta, { Default } from './ManagerText.stories';
import Links from '../../../.storybook/docs/external-links';

<Meta title="Components/Manager Text/Documentation" />

<StoryBookBanner of={meta} />

<Canvas layout="centered" of={Default} sourceState="none" />

<StorybookHeading label="Overview" level={2} />

<StorybookIdentityCard
  githubUrl={Links.github.ManagerText}
  name="ManagerText"
  relatedComponents={[
    { name: 'ods-text', href: Links.ods.text },
    { name: 'ods-tooltip', href: Links.ods.tooltip },
  ]}
>
The **Manager Text** component displays a text with a tooltip for additional context. It is designed for interactive usage, with mandatory URN and IAM actions properties to associate the text with external resources.

The component supports internationalization (i18n).

</StorybookIdentityCard>

<StorybookHeading label="Anatomy" level={2} />

1. **ODS Text**: it is restricited to the "span" preset; it serves as the trigger for the tooltip
2. **ODS Tooltip**: it displays contextual information about the text

<StorybookHeading label="Usage" level={2} />

Use the **Manager Text** to display a concise text requiring additional explanation via a tooltip.

<StorybookHeading label="Dos & Don'ts" level={2} />

<StorybookBestPractices
  dos={[
    'Do use the Manager Text for succinct, (inline?) content requiring contextual explanations',
    'Do provide valid URN and IAM actions',
  ]}
  donts={['Do not use the Manager Text for purely decorative purposes']}
/>

<StorybookHeading label="Placement" level={2} />

The **Manager Text** component is typically used inline within a table (a paragraph?), or alongside other UI elements where additional explanation is necessary.

<StorybookHeading label="Behavior" level={2} />

**States**

- **Default**:
  - The text is visible, and the tooltip is hidden by default
- **Hover and focus**:
  - All inherited from the ODS Tooltip component

**Interactions**
Hovering or focusing over the text displays the tooltip.

**URN**: links the text to an external resource.

**IAM actions**: related to IAM policies; One or more **actions** allowed or excepted by an IAM policy.

<StorybookHeading label="Variation" level={2} />

**Internationalization (i18n)**

The **Manage Text** component supports the following languages: English (default?), German, Spanish, French, Italian, Dutch, Polish and Portuguese.

<StorybookHeading label="Accessiblity" level={2} />

_Ensure the component meets accessibility standards.
Describe what should keyboard interaction do to the component
WAI Patterns can help you go though keyboard interaction :_ **[https://www.w3.org/WAI/ARIA/apg/patterns/](https://www.w3.org/WAI/ARIA/apg/patterns/)**

<StorybookTable
  data={{
    columns: ['key', 'interaction'],
    rows: [
      {
        key: 'Tab',
        interaction: 'Move focus to the text; The tooltip is displayed',
      },
    ],
  }}
/>
