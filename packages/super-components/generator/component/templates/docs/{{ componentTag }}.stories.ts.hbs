import { setupWorker } from 'msw';
import handlers from '../mock/handlers';
import { I{{ pascalCase componentTag }} } from '../src';

setupWorker(...handlers).start({ onUnhandledRequest: 'bypass' });

export default {
  title: 'Components/{{ name }}',
  tags: ['autodocs'],
  render: ({ content }: I{{ pascalCase componentTag }}) => `
    <section>
      <{{ componentTag }} content="${content}" />
    </section>
  `,
  argTypes: {
    content: { control: 'text', default: 'World' },
  },
};

export const StoryExample = {
  args: {
    content: 'Another Example',
  },
};
