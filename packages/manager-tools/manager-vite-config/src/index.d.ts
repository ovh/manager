export declare const getBaseConfig: (config: {
  local2API?: boolean;
  isLABEU?: boolean;
  host?: string;
  devLoginUrl?: string;
  baseUrl?: string;
  authURL?: string;
  proxy?: {
    host: string;
  };
}) => any;
